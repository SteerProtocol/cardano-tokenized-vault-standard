# Prior illustrated edition

This directory is a read-only historical snapshot of the 16 September 2026 illustrated publication. It preserves the old Markdown sources and unillustrated revision-0.6 baselines, exact PDFs, illustrative integration schema and responses, machine-readable evidence snapshots, human-readable validation notes, dated checks, and generated figure previews. These files let readers inspect what that edition said and reproduce or understand its historical publication checks. They are not inputs to the current whitepaper build or maintained current integration contracts.

The later reference-guide PDFs for that edition are in `delivery/implementation/`. The original combined PDF has SHA-256 `e06e90a6eb2eb1e9ffdc90d38b7bcabd392b54b3144d78faf11c8be321054e75`, the same bytes cited by the repository's earlier conformance reviews. `evidence/CTVS-Validation-Evidence.md` is the human-readable companion to the archived JSON evidence and remains at the path expected by the historical checker.

The files are a dated record. The current papers and maintained reference implementation are linked from the [whitepaper README](../../README.md). The archived response schema was not reconciled byte for byte with the later implementation reader, so it is not a maintained integration contract.
